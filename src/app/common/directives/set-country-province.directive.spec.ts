/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SetCountryProvinceDirective } from './set-country-province.directive';

describe('SetCountryProvinceDirective', () => {
  it('should create an instance', () => {
    const directive = new SetCountryProvinceDirective();
    expect(directive).toBeTruthy();
  });
});
